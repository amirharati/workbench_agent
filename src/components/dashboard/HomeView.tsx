import React from 'react';

export const HomeView: React.FC = () => {
  return (
    <div
      style={{
        minHeight: 260,
        border: '1px dashed #d1d5db',
        borderRadius: 10,
        background: '#ffffff',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: '#6b7280',
        fontSize: '0.95rem',
        padding: '1rem',
      }}
    >
      Home is intentionally empty for now.
    </div>
  );
};
