# Codex Sessions

## 2026-07-14 - Review Cursor pipeline/digest handoff
- Session ID: `019f5f16-5b42-7260-a00c-b77c4dd4dbbf`
- Parent Session ID: `334112f6-df29-412a-8a9d-6b769d3f99b2` (Cursor handoff)
- Task title: `Review Cursor pipeline/digest handoff`
- Surface: CLI
- Project path: `/Users/amir/projects/personal_tools/workbench_agent`
- Original cwd: `/Users/amir/projects/personal_tools/workbench_agent`
- Resume: `codex resume 019f5f16-5b42-7260-a00c-b77c4dd4dbbf`
- Summary: Reviewed the Cursor transcript and commit `5620400`, then implemented the corrected execution boundary: serialized bulk jobs run in the offscreen realm while singles remain immediate in the page's high-priority lane. Added duplicate-bulk protection, runtime progress/cancel messaging, hard-cancel propagation, priority forwarding, and a DB bootstrap readiness barrier. Replaced full-library offscreen hydration and full-mirror handoff with a canonical DB-worker seed scoped to the requested item IDs, avoiding Chrome's 64 MiB runtime-message cap. Removed the subsequent global category-link hydration bottleneck as well: SQLite now computes leaf and parent assignment aggregates and returns only compact count rows. Standardized interactive durability across side panel and main-panel CRUD: bookmark add/edit/move/delete and bookmark notes, projects, collections, workspaces, snapshots, trash history, and category review now wait for DB-worker acknowledgment before success notifications. Buffered write errors propagate, item mutations get canonical SQLite read-back verification, and ordinary UI RPCs are tested as high priority while offscreen bulk retains low priority. Single-digest completion also emits a refresh after its in-flight guard clears so committed AI data appears immediately. Fixed cold side-panel reopening independently of bulk: saved-state detection now performs a high-priority indexed URL query directly against the DB worker, digest context loads through a one-item canonical worker seed, and the UI shows `Checking saved state...`/`Already saved` instead of a false Save action while full library metadata hydrates.
- Commits/PRs: Reviewed `5620400`; committed `d2dbba5` (`Fix verified/enriched status display`) and `77e7c72` (`Commit pipeline durability and side panel fixes`).
- Status: Implemented and committed. Production build passes. Bulk runs now stay in the offscreen lane, interactive writes use the DB-worker durability path, cold side-panel reopen reads saved state directly from the worker, and accepted categories now surface as `Verified` while meta-only signal caches still count as fully enriched. Browser dogfood beyond the user’s manual verification is still pending because no runtime is attached here.
- Next steps: Reload the unpacked extension and continue dogfooding bulk + single digest concurrency, side-panel reopen/save behavior, and the exact semantics of `Enriched` vs `Verified` counts if product wording still needs adjustment.

## 2026-07-16 - Plan UI redesign
- Session ID: `019f6b7e-5590-7600-9446-7f25d0454929`
- Task title: `Plan UI redesign`
- Surface: Desktop
- Project path: `/Users/amir/projects/personal_tools/workbench_agent`
- Original cwd: `/Users/amir/projects/personal_tools/workbench_agent`
- Resume: `codex resume 019f6b7e-5590-7600-9446-7f25d0454929`
- Latest update: Project Home now has one unambiguous Project view tab strip instead of separate Browse and Working set rows. All items, Pinned, Collection, and Workspace are mutually exclusive views, and the highlighted tab always names the list rendered in the left pane. Home's All Library context now uses the same model adapted to its role: Projects, Recent, Favorites & pins, and Workspace are mutually exclusive views in one bounded two-column canvas beneath Find; the old separate Continue working and Browse library sections are consolidated. The standalone Library page separately uses All items, Links, and Notes views with search and scope as subordinate filters. Project Home, Home All Library, and standalone Library share the List/Gallery browser and editable item workspace, with independently remembered display modes. Tab Commander now has a purpose-built full-page presentation while preserving the reusable bottom-panel variant: the page removes the inert drag/collapse chrome and duplicate capture action, adds the standard heading/filter/bounded-canvas spacing, makes window versus individual-tab capture precedence explicit, keeps window labels/order stable during the session, persists List/Gallery choice, exposes tab selection/actions without hover, and adds keyboard activation semantics. A stronger uncommitted Tab Commander iteration now separates Live windows and Tabs in selection into distinct panels, replaces expandable window cards with compact stable selectors and inline actions, restyles tab rows/gallery as scalable browse surfaces, initializes the current window without a selection flash, turns individual-tab selection into a contextual action state, and makes the single Capture label reflect its actual target. Its live Windows/Tabs workflow remains distinct from the durable saved Workspaces page. Legacy project render blocks remain available during UX evaluation for easy rollback.
- Enrichment Hub update: Reframed the page as a library-first, full-page review workspace. Enrichment review, Classification review, and Taxonomy are now peer destinations instead of nested debug lanes; direct sidebar entry resets to All Library while explicit scoped deep links remain possible. The enrichment status controls are consolidated, selection and user-triggered reruns remain intact, and selecting a row opens a persistent bounded inspector beside the list instead of expanding technical detail inline. The inspector is result-first: AI summary/tags precede fetch and index diagnostics, classification remains visible, and raw fetch/debug data is collapsed and read from disk only when opened rather than occupying a sticky overlay. No automatic or speculative AI suggestion workflow was added: costly pipeline work remains explicitly initiated by the user. Focused navigation tests and the production build pass. Committed as `4a13d13` (`Redesign Enrichment Hub review workflow`).
- Import Studio update: Rebuilt the page around a staged ingestion workflow: choose Bookmark file or Chrome bookmarks, review a scalable filterable list with explicit row selection and one shared project/collection destination, save to the library without network or AI work, then explicitly choose whether selected saved links should be processed. Invalid URLs are excluded from the default selection; Inbox/Incoming and project/Unfiled labels match the current data model. Switching source resets stale candidates, the future Format assistant remains visible but is clearly labeled Not implemented with disabled controls, the duplicate enrichment control is removed, and the post-save processing stage replaces the review list. The selected-count Import and Process selected actions are visible at the top-right of their respective Review and Process headers rather than below long lists. Processing shows a determinate, monotonic progress bar plus its detailed phase text. Resumable pipeline state is created only when processing is confirmed, not merely because a large import was saved. Focused workflow tests and the production build pass; changes remain uncommitted for UI review.
- Settings review: Committed the Import Studio iteration as `c3c58c6`, then audited the full Settings surface without changing it. Settings currently combines General preferences, browser-home setup, detailed AI credentials/routing/test controls, operational enrichment actions, taxonomy setup and resets, pipeline debug capture/export, backup sync/conflict resolution/history/restore/export, and full-library deletion in one long light-only column. Recommended first iteration: peer views for General, AI & processing, Backup & restore, and Advanced; default to Backup when setup or conflict needs attention; use shared design tokens; keep ordinary AI connection fields primary and collapse expert routing/test parameters; remove duplicate operational enrichment controls in favor of Enrichment Hub; retain taxonomy/debug/export under Advanced; and isolate clear-library actions in a real Danger zone with current Inbox/Incoming terminology.
- Settings implementation: Replaced the single long Settings column with General, AI & processing, Backup & restore, and Advanced destinations. Backup opens first when folder protection needs attention and now distinguishes automatic live mirroring/rotation from manual snapshots and restore points. Operational enrichment controls were removed from Settings in favor of Enrichment Hub. Confirmed the bundled taxonomy already auto-seeds locally at startup when no leaves exist, made the taxonomy maintenance view enforce the same safe guard, replaced the misleading manual-import setup language with an explicit confirmed starter reset, and clarified that complete library backups preserve customized taxonomy. Updated stale empty-taxonomy guidance and clear-library terminology to Inbox/Incoming. Focused Settings/Hub tests and the production build pass; changes remain uncommitted for UI review.
- Settings/pipeline follow-up: Converted Settings cards, neutral text, borders, native inputs, selects, textareas, status panels, and restore controls from hardcoded light colors to shared theme tokens so every Settings destination follows dark/light mode. Fixed Import Studio cancellation to send the hard-cancel reason instead of creating a soft resumable pause. Cancelled persisted jobs are now reconciled as terminal and cleared, while genuinely partial jobs remain recoverable but are labeled as saved checkpoints with an explicit remaining count and clear Resume-versus-Dismiss guidance instead of appearing to be actively paused. Production build and focused lifecycle/UI tests pass; changes remain uncommitted for review.
- Theme, readability, and side-panel implementation: Committed the Settings/taxonomy/pipeline iteration as `58fca45`. Added a persisted Dark/Light selector to Dashboard Settings → General → Appearance; it initializes before the dashboard renders and synchronizes dashboard windows. The side panel deliberately keeps browser/system `Canvas` colors and native control color scheme so the dashboard preference does not make it look foreign beside the current site. Rebuilt the side panel as a contextual capture surface: a sticky header keeps Add link and Dashboard visible, one canonical current-page card handles save/edit, membership is described as saved locations rather than copies, unsaved pages default to Inbox/Incoming with optional organization, saved pages expose Favorite and location management, and external-link capture is a compact secondary form. Both initial page capture and secondary link capture can target any number of existing project collections and can create a new project or non-Inbox collection inline; the same shared organization editor handles subsequent saved-location changes. Removed duplicate saves, ambiguous Pin, browser-home setup, copy/delete administration, and automatic paid digest runs from all side-panel save paths. AI digest is explicitly initiated, compact by default, expandable inline using the panel's outer scroll, and links to the full dashboard. Completed a source-level dark/light readability audit: raised compact type minimums, brought muted/faint text to WCAG-AA contrast, strengthened borders and control surfaces, added visible keyboard focus, introduced theme-aware status colors, made `+ New` destination controls high-contrast, corrected light-only add/edit/debug surfaces, and made search, collection, resizer, drag-preview, warning, and system-side-panel scrollbar styling theme-safe. Browser screenshot inspection was unavailable in this session; focused tests and the production build pass. Committed as `21d7530` (`Redesign side panel and unify theme readability`).
- Dashboard design unification: Added one structural UI pattern layer for page frames, headers, descriptions, action rows, toolbars, view tabs, panels, split canvases, empty states, buttons, inputs, and dialogs. Migrated the active Home/Project/All Library, standalone Library, editable Item, Workspaces, Tab Commander, Enrichment Hub, Import Studio, Settings, Help, favorites/recent/pinned/trash utility lists, shared content browser, and creation/confirmation modal surfaces to those patterns. This deliberately standardizes hierarchy and interaction density without beginning the planned typography/font polish. Fixed Library pipeline-status labels overlapping titles by allowing the shared leading slot to size to full badges while retaining a compact minimum for ordinary icons. Corrected startup after comparing against `2a2976a`: the durable Chrome snapshot and shared DB-owner snapshot now race in parallel; startup projection v2 strips browser-window/tab payloads; and a synchronous first-paint mirror seeds compact content before canonical hydration. The remaining Library-only delay was traced separately: every dashboard had been hydrating/transferring complete pipeline tables merely to derive small status badges, while mounting the complete Library DOM at once. DB-owner protocol v4 now computes and revision-caches scoped badge results centrally so additional dashboards receive only compact badge entries, and shared content browsers mount the first 60 entries immediately then reveal the rest in idle-time chunks with Chromium rendering containment. Added persisted page-navigation state beyond the already-persisted shell route/scope: Library restores query, Links/Notes filter, selected item, and workspace destination; Home restores its All Library subview, workspace grouping, and selected item/entry for the current scope; each project/collection canvas restores browse source, selected item, or workspace entry; Home search and promoted working search independently restore query, filters, results, search mode, and selection. Restored selections rehydrate the inspector, invalid/deleted IDs are discarded when canonical data is available, and progressively mounted lists scroll the restored row into view. Twenty-seven focused tests and the production build pass. Browser screenshot QA remains unavailable. Changes are uncommitted pending user review.
- Page-state restoration follow-up: Home and Library/Notes now use separate persisted keys for every project/collection scope, keyed page mounts restore the correct state when scopes change, and project workspace hydration no longer overwrites a restored item or workspace-entry selection. Added direct render tests proving Library and Project pages reopen with their stored item selected. Sixteen focused state/component tests and the production build pass; changes remain uncommitted for user testing.
- State-restoration performance follow-up: Split Search persistence into a compact startup record (query, filters, mode, selected ID, and empty-index flag) and a separate result cache. Both Home Search and promoted Search now paint from compact state, then parse cached results and restore the selected inspector item during idle time; result serialization after a completed search is also idle-scheduled. Active promoted-search tabs wait for cache hydration and rerun only after first paint when no matching cache exists. Ordinary page-state saves and compact Search saves compare serialized state and skip unchanged mount-time writes. Search storage keys moved to v2 while still in development. Added a lightweight “Restoring previous results” status. Thirty-eight focused startup/state/component tests, the production build, and `git diff --check` pass. Browser timing remained unavailable because no browser-control target was connected; changes remain uncommitted for user testing.
- New-dashboard startup follow-up: Identified a concrete cross-document readiness race: each new dashboard started with a local DB-owner-ready flag unset and could wait for a one-shot broadcast even after the already-open shared offscreen DB owner had successfully answered its ping. A successful routed ping now marks that dashboard ready immediately, and concurrent startup callers share one in-flight handshake instead of sending duplicate pings. Heavy non-landing routes (Settings, Import Studio, Enrichment Hub, Tab Commander, Workspaces, Collections, legacy project/note surfaces, and enrichment controls) are route-lazy; Enrichment Hub prewarming moved behind canonical library readiness. The initial main bundle fell from about 1.10 MB / 276.85 KB gzip to 637.86 KB / 169.53 KB gzip, while Home and Library stay immediate. Forty-six focused tests, the production build, and `git diff --check` pass; the only test output is the existing Tab Commander server-render `useLayoutEffect` warning. Changes remain uncommitted for user timing verification.
- Cold cached startup follow-up: A durable immediate projection now owns the first usable render even when no other dashboard is open. When either the synchronous page projection or durable Chrome projection is available, the shared DB owner is warmed immediately but canonical library transfer waits for a bounded idle window (350 ms maximum) so it does not compete with the cached paint; a truly uncached/first-install startup still hydrates immediately. Browser window/tab enumeration and AI settings loading also moved to bounded idle work (500 ms maximum), while their listeners remain active. Added a shared startup-idle scheduler with fallback and idle-callback tests. Forty-eight focused tests, the production build, and `git diff --check` pass; initial main remains about 638.49 KB / 169.72 KB gzip. Changes remain uncommitted for cold/warm timing verification.
- Design-foundation audit: Committed the complete design-unification, persisted page-state, and startup-performance batch as `6d06352` (`Unify dashboard design and accelerate startup`). The active pages now share a coherent structural grammar and theme-safe semantic color layer, so the product is ready for a focused system-hardening pass followed by visual polish rather than another UX redesign. Before detailed polish, consolidate the duplicate CSS/TypeScript token sources around CSS variables, promote the most repeated interactive patterns into stateful components (buttons, fields, tabs, panels, empty states), define responsive collapse and reduced-motion behavior, centralize overlay layers and semantic danger/favorite colors, and remove tracked conflicted-copy/legacy source where safe. Typography, exact spacing, icons, shadows, and microcopy remain ordinary polish work.
- Design-system hardening pass: Consolidated the UI foundation around `global.css` variables and removed the duplicate `src/styles/theme.ts` token object. Added shared spacing/control/motion/layer and semantic danger/favorite/live tokens; class-backed button, field, tab, and panel states; narrow-window collapse rules for active split canvases; horizontally scrollable compact tab bars; and `prefers-reduced-motion` support. Connected the active Library, Home All Library, Project, Workspaces, Import Studio, Enrichment Hub, Settings, Help, Tab Commander, content browser, favorites, trash, modal, and overlay paths to the new grammar. Replaced active hardcoded danger/favorite/live colors and numeric overlay layers with semantic variables. Removed the unreferenced legacy `NotesView` and the tracked BottomPanel conflicted copy. Added primitive contract tests and updated the Project layout contract test. Workspace detail lists now render URL-bearing Homebase items and direct URL entries as explicit keyboard-accessible links; browser-snapshot URLs use the same treatment. Twenty-six focused tests and the production build pass; the additional workspace-link test and final TypeScript check also pass. Only the pre-existing Tab Commander server-render `useLayoutEffect` warning and existing Vite chunking warnings remain. Committed as `1492365` (`Harden dashboard design system`).
- Global polish layer 1: Refined the shared type hierarchy and system-font stack, promoted page titles to a clearer 20px-scaled level, raised page descriptions to the readable small-body level, and added explicit leading/tracking tokens. Added theme-aware raised/subtle surfaces, stronger borders, placeholder colors, and native `color-scheme`; panels now sit on a raised surface while tab bars and panel headers use a quieter subordinate surface. Refined class-backed controls with more deliberate secondary/primary/icon treatment, primary depth, pressed feedback, theme-aware placeholders/carets, and shared URL-link affordance. Fixed side-panel theme mixing exposed by the new surface tokens: extension surfaces now use an explicit `?surface=side-panel` identity instead of viewport-width inference, and a complete Chrome/OS-derived token scope lives on the document root so the page background, portalled overlays, fields, status colors, and saved-card scrollbars cannot inherit dashboard-dark values. The dashboard preference remains independent and no extra side-panel theme setting was added. Twenty-seven focused UI tests, seven focused theme/surface/primitive tests, and the production build pass. Committed as `Polish global UI and isolate side-panel theme`; shared list/panel polish remains next.
- Shared browsing polish: Reworked the active `ContentBrowser` used by Home All Library, standalone Library, and Project canvases into a class-backed list/gallery system with a clearer title/count header, compact mode selector, stronger title/subtitle/meta hierarchy, scalable gallery cards, calmer hover/selected/focus states, and visible-but-secondary row actions. Action keyboard events no longer bubble into row selection. Aligned the adjacent detail panels and editable Item workspace: consistent raised/subtle surfaces, clearer item titles, grouped Organization/URL/Notes sections, readable note bodies, responsive action wrapping, and semantic saved status. Favorites, Recent, Pinned, Quick Access, and Trash now share compact scalable utility rows with keyboard activation and isolated actions. Nineteen focused browser/inspector/page tests plus the production build pass. Committed as `Polish shared browsing and item workspaces`.
- Shared forms and feedback polish: Added a reusable accessible dialog shell with portal layering, Escape handling, focus containment, initial focus, opener restoration, responsive sizing, and theme-token surfaces. Creation flows, enrichment/classification confirmations, pipeline maintenance and import dismissal, Trash deletion, and specialized batch/retry dialogs now share the same header, form, action, warning, and destructive-action grammar. Project/collection organization and project workspace naming use shared fields and inline forms; Workspaces no longer relies on native prompt/confirm dialogs for create, rename, or delete. Status bars and out-of-sync feedback use semantic info/warning/error tokens in both themes. Eleven focused component tests, TypeScript, `git diff --check`, and the production build pass; only existing Vite chunking warnings remain. Committed as `Polish shared forms and dialogs`.
- Interactive-create performance diagnosis: The forms/dialog commit did not modify persistence behavior. Link, note, project, and collection creation all block their modal on the shared App handlers, which await worker durability and then explicitly call full `loadData`; the synchronous same-document data-change subscriber launches another `loadData` for the same mutation. Each load repeatedly reads the persisted backup-folder handle and republishes all projects, collections, workspaces, and active items. Worker mutation broadcasts then schedule a 750 ms revision catch-up that can force essential plus pipeline-table hydration because ordinary `storeInvoke` acknowledgements do not advance the tab cache revision, while the debounced full SQLite folder mirror can contend with later writes. Recommended fix: keep one OPFS-worker acknowledgement as the durability boundary, patch created entities into React state surgically, remove duplicate full reloads, return/set the worker revision on mutation acknowledgements so own writes do not rehydrate, and leave folder mirroring asynchronous and singly owned by the DB worker.
- Interactive-create performance implementation: Kept the existing offscreen SQLite Web Worker as the single durability thread and upgraded its mutation protocol to return the acknowledged database revision. Link, note, project, collection, and workspace writes now update the worker-backed read cache surgically instead of blocking on one or more full-library hydrations; project plus required Unfiled collection creation is one atomic transaction. Scoped change events carry entity ID, worker revision, and document identity, so the originating page ignores its own broadcast while peer dashboards refresh only the affected item or small table. Removed redundant App/side-panel/legacy-project refreshes and duplicate folder-flush scheduling; the DB worker alone schedules the asynchronous folder mirror after OPFS commit. Hot `getDB` calls no longer reread the directory handle. Side-panel creates still await canonical SQLite commit and item verification before reporting success. Twenty-three focused tests, TypeScript, `git diff --check`, and the production build pass; the user reloaded the extension and confirmed the latency improvement before commit.
- Dashboard shell polish: Replaced the remaining one-off shell styling with a shared class-backed navigation system. The main sidebar now has clearer brand/scope/navigation hierarchy, compact count badges, theme-safe hover/active/focus states, responsive width behavior, and a single-pass collection-count map instead of rescanning every item for every collection. Project and collection create/delete flows now use the shared accessible DialogShell and accurately explain reassignment behavior. Scope filters render as a labelled, dismissible trail. Inspector/Ask and Open Work use explicit tablist/tab semantics, keyboard activation, named close actions, coherent active surfaces, and responsive constraints; the shell containers themselves now use semantic main/navigation structure. Twenty-two focused shell/dialog/primitive tests, TypeScript, `git diff --check`, and the production build pass. Browser screenshot QA was unavailable because no browser surface was attached. Committed as `460c86c` (`Polish dashboard shell navigation`).
- Home project/material hierarchy follow-up: Removed Projects from the same selectable two-column browser used by links, notes, favorites, and workspace entries. All Library Home now has a separate bounded “Open a project” navigation region whose copy explicitly states that opening a project changes Home context without modifying a workspace. The launcher scales through a bounded vertical browser, project-name/description search, and All/Recent filters. All preserves the stable project source order; Recent uses a separate persisted newest-accessed history so opening a project never reorders the manually arranged five-project scope switcher. Search always covers the complete project catalog, and the launcher filter/query restore with the rest of Home page state. The working canvas is now labelled Library material and contains only Recent, Favorites & pins, and Workspace views. Item selection consistently opens editable details with an explicit Add to global action; workspace-entry selection exposes Focus; project cards expose explicit Open navigation. Persisted legacy `projects` material-view state normalizes to Recent, and older navigation state seeds the new access history from its existing switcher once. Nineteen focused launcher/scope/navigation/page-state tests, TypeScript, `git diff --check`, and the production build pass. Committed as `72b7451` (`Clarify Home project and material navigation`).
- Responsive-layout implementation: Replaced the viewport-only dashboard fallback with a shared system based on the actual remaining workspace container width plus independent compact-height rules. Compact laptop mode reduces shared frame gaps/gutters, clamps secondary page descriptions, keeps actions/tabs/toolbars on one accessible horizontal row, shrinks shared list/gallery density without changing font scale, turns Home projects into slim 44px cards in a one-complete-row internal scroller, and shortens the Home working canvas. The launcher now sizes its row viewport, cards, minimum card width, icons, padding, and gaps as one responsive unit; roomy mode shows exactly two rows, compact mode exactly one, and mandatory row snapping prevents the next project from being left partly covered. At <=1320px the Inspector overlays the workspace rather than permanently consuming 280px. At narrow workspace widths, Home/Project/Library/Workspaces use a reusable one-pane browse/detail canvas with an explicit Browse return; Workspaces no longer immediately reselects the first row after that return. Enrichment review uses the same list-versus-inspector model at its table-safe breakpoint, Tab Commander uses bounded stacked live-window/tab panes, Import moves its destination sidecar above the review list, and Settings form grids collapse to one column. Full-page routes now have one padding owner instead of shell-plus-page double padding, with Settings and Trash connected to the shared page frame. Chrome-safe bottom space is now an unconditional shell invariant on large and small screens: the dashboard body physically ends 64px plus the platform safe-area above Chrome's native new-tab footer, while every shared page frame, Home overview, and independently scrolling Search surface keeps another 24px content margin. The Inspector overlay and fixed toast stack respect the same shell boundary; the former compact-mode `56px` override was removed. The latest focused responsive/page suite has 29 passing tests, TypeScript, `git diff --check`, and the production build pass. Live screenshots were unavailable because no browser surface was attached; manual extension verification at laptop dimensions remains pending. Committed as `Adapt dashboard layouts across screen sizes`.
- Bottom-clearance follow-up: Moved Chrome's native-footer clearance from only the middle/right body to the outer dashboard shell so the sidebar, pages, and inspector share one protected viewport on both large and small screens. Increased the protected footer band to 88px plus platform safe area, retained a separate 24px in-page breathing margin, switched shell sizing to dynamic viewport height, removed the inspector's duplicate footer subtraction, and made the shared MainContent/full-page flex boundary shrink correctly when an import/status banner is present. Nine focused responsive/content tests, `git diff --check`, and the production build pass. Browser screenshot verification remains unavailable because no browser target is attached; the code is included in `4918027` and awaits manual extension review.
- Final polish completion pass: Standardized the remaining active Search/Focus and Settings controls on semantic theme tokens and shared button/card states. Replaced native destructive/cost confirmations across library items, workspaces, projects, collections, enrichment clearing, categorization reruns/resets, debug purge, and folder-snapshot restore with the accessible shared dialog; removed the prior double-confirm collection path. Tightened the browser side panel without coupling it to the dashboard theme: stable full-height scrolling, semantic saved/pending status, true Favorite coloring and pressed state, consistent fields/actions, and success feedback. Source review found only App-level backup-file replacement/version-override system prompts still using native confirmation. Thirty-seven focused tests, TypeScript, `git diff --check`, and the production build pass. The repository-wide Vitest invocation still reports its known standalone-script empty suites and the unrelated image-only X categorization assertion; 44 normal suites/165 tests pass. Browser screenshot QA remains unavailable because no browser target is attached. Committed as `Complete dashboard and side panel polish`.
- All Library compact-layout follow-up: Removed the redundant Overview search control so the permanent Search section is the single search workflow, and replaced the always-expanded project-card block with a one-row recent-project switcher plus an explicit searchable All projects disclosure. Removed the redundant Library material heading and gave the material browser the reclaimed responsive height. Corrected Chrome-footer protection so the dashboard shell remains full height and clearance belongs to scroll-content ends rather than a fixed white footer-like band; the extra page margin and duplicate nested All Library clearance were removed so only the minimum Chrome-safe space remains. Replaced the nearly invisible hover-only Inspector handle with a persistent labelled rail that explicitly reopens the panel after collapse. Inspector now remains in normal flex layout at laptop widths and narrows responsively, so opening it compresses rather than overlays the workspace. The resulting workspace-width change initially triggered the one-pane browse/detail mode too early; its breakpoint is now 560px instead of 720px and compact Inspector widths were reduced, preserving Incoming/Pinned browsing on laptop screens. Shared page titles were reduced from 20px to 18px while view tabs increased from 12px to 13px; Project Incoming, Pinned, Collection, and Workspace compound controls follow that same readable tab scale. Fifty focused Home/library/layout tests, TypeScript, `git diff --check`, and the production build pass; the latest breakpoint correction has twenty-one passing focused checks plus a successful production build. Live browser QA remains unavailable because no browser target is attached. Committed with the responsive Home checkpoint.
- Project workspace compact follow-up: Workspace-only commands no longer join the Project view tablist. Save, Include global, Open links, Focus, and Delete render in a separate bounded horizontal action toolbar below the persistent Incoming/Pinned/Collection/Workspace row, preventing selection of Workspace from scrolling the entire navigation line out of view on compact screens. The Project view ribbon now spans the full workspace and, below 1000px, uses a 46px ribbon with 34px navigation controls so it has appropriate visual weight relative to the project header and Workspace action ribbon. Twelve focused checks, TypeScript, `git diff --check`, and the production build pass; committed with the responsive Home checkpoint.
- Complete-library hierarchy follow-up: All Library now opens on the complete material catalog rather than a misleading recent subset, with All, Links, and Notes as subordinate filters and Favorites/pins and Workspace as peer views. The expanded project-card region is replaced by recent-project shortcuts and a searchable, keyboard-dismissable vertical All projects popover that does not consume canvas height or alter workspace membership. Home's section and scope navigation share one compact context row. Compact Project pages hide the duplicate large heading, retain Search in the view strip, and move workspace-only commands into the material-list header instead of dedicating another permanent ribbon. Fixed All Library height overrides were removed so reclaimed space goes to the list/editor canvas on smaller screens. Legacy persisted `recent`/`projects` All Library views normalize safely to All. After manual review found the first control bar over-compressed, project navigation and material views were rebalanced into two purposeful rows; recent-project names now remain complete in a bounded horizontal scroller and full-list project names wrap instead of truncating. Fifty-six focused tests, the final twenty-one responsive checks, TypeScript, `git diff --check`, and the production build pass; changes remain uncommitted after checkpoint `e89c443` pending manual responsive review.
- Item-icon correction: Fixed the empty square rendered for ordinary All Library items. The shared leading container now always contains a Link or Note icon, with Favorite/Pin markers retained only as secondary indicators; shared item-type icon contrast is stronger in Home and Project. Twenty-two focused tests and TypeScript pass. Committed as a manual-testing checkpoint after `Refine All Library hierarchy and project browsing`.
- Sidebar/project scope consistency: Made explicit scope navigation authoritative over remembered Project canvas state. Selecting a project from the sidebar opens its All-items view; selecting a collection opens that collection; clicking either again resets the middle pane to the matching material view instead of leaving a stale Pinned or Workspace lens active. Inbox no longer exposes redundant All and Incoming rows: it shows one Incoming destination mapped to the canonical project-wide scope, while its concrete default collection remains the save destination. Persisted legacy Inbox collection scope is normalized to canonical Incoming automatically. The navigation request is transient, so ordinary reload restoration of Pinned/Workspace and selected-item state remains intact. Twenty-seven focused scope/shell/project tests, TypeScript, `git diff --check`, and the production build pass; changes are uncommitted pending manual review.
- Cross-project workspace destination picker: Replaced All Library's global-only action and Project's expanding destination select with one compact `Add to workspace…` trigger and accessible bounded dialog. The picker keeps the current context first, remembers five recent targets, searches full `Project / Workspace` paths, groups every remaining destination by project, shows per-destination Added state, supports several additions without leaving the item, and does not alter durable project/collection membership or navigate/activate the target. Destinations include Global, every project Live session, named Homebase workspaces, and browser-derived working copies. Adding to a never-activated browser destination safely seeds its mutable working copy with the captured tabs before inserting the item, leaving the original browser snapshot unchanged. Recent targets persist with Home workspace state. Sixty-seven focused workspace/picker/Home/Project/shell tests, TypeScript, `git diff --check`, and the production build pass; committed as `5ceff9e` (`Add cross-project workspace destination picker`).
- Search organization and Home hierarchy follow-up: Selected results on global, project, and collection Search; focused/saved Search tabs; and the standalone Search route now keep the temporary `Add to workspace`/open action beside one compact `Organize…` control. The latter opens the shared accessible organization editor in a bounded dialog, uses the complete project/collection catalog even when search filters are scoped, shows current permanent locations, supports adding/removing memberships and inline destination creation, and explicitly states that workspace membership remains separate. Corrected `showTags={false}` in the shared editor so destination-only surfaces show saved locations without an unrelated tag field. Overview and Search now live beside the existing Home label in the primary sidebar navigation rather than creating another content ribbon; the content-level ribbon contains only All Library and project scopes, and duplicate Search controls were removed from the Project heading/material row. Fixed the project-to-All-Library scope transition by resolving search result IDs against the complete item catalog instead of the active project's subset, so global results selected from a project context retain both Add to workspace and Organize actions. Forty-five focused Search/Home/Project/workspace/scope/responsive tests, TypeScript, `git diff --check`, and the production build pass; changes are uncommitted pending manual review.
- Home navigation placement correction: Reverted the mistaken Overview/Search placement inside the left sidebar. The sidebar again has one primary Home destination; the page-title row now reads `Home · Overview · Search`, while the separate row below remains the All Library/project context switcher. This preserves the intended hierarchy between Home views and workspace/project scope. Nineteen focused shell/Home/Search/responsive tests and the production build pass.
- Project-to-All-Library Search correction: Search presentation no longer remains artificially limited to the project from which Search was opened after the user selects All Library. The result UI resolves against the complete item catalog, its collection filter follows the selected search scope, and a selected result now exposes both permanent `Organize…` membership and the full cross-project `Add to workspace…` destination picker while leaving Home in the originating project context. Eleven focused Search/workspace/project tests and the production build pass; changes remain uncommitted pending manual review.
- Search organization context defaults: Search now passes its originating project and collection separately from the selected query scope. Opening `Organize…` after widening a project Search to All Library preselects the original project and collection as the proposed permanent destination; project-only context chooses that project's default/Unfiled collection before arbitrary alternatives. Existing Saved in memberships remain untouched until Add is pressed. The same behavior applies to Home Search, focused saved Search tabs, and the standalone Search route. Nineteen focused tests and the production build pass.
- Shared gallery-card layout: Reworked ContentBrowser gallery cards into four purposeful full-width rows: status/type labels first, title second, URL/summary third, and metadata/actions together in the footer. Wide badges such as Enriched can no longer compress the title to a few letters; URL/summary copy uses the full card width for up to four lines. Removed the former blanket 43px action reservation, increased normal/compact card heights to 164/144px, and preserved the existing list presentation. Thirty-five focused ContentBrowser/Home/Project/Library/responsive tests and the production build pass; committed with the compact Item-details pass as `84ca0d7`.
- Item-details compact-layout follow-up: Made the shared detail pane and Item workspace responsive to their own rendered widths rather than the browser viewport. Home, Project, and Library detail toolbars now wrap and relax select widths; detail padding tightens without sacrificing Chrome-footer clearance; item title/actions stack; organization destinations become a single-column form with a full-width Add action; inline creation, tags, notes, errors, and long content stay bounded. Preserved the previously reviewed 560px one-pane browse/detail breakpoint so laptop browsing is not hidden prematurely. Thirty-three focused detail/Home/Project/Library/responsive tests, `git diff --check`, and the production build pass. The repository-wide Vitest command still reports its known standalone-script empty suites and unrelated categorization assertion while all 187 registered tests pass; committed as `84ca0d7`.
- Shared in-list quick filtering: Added a compact, clearable filter to shared list/gallery browsers, Home Recent/Favorites/Projects, active workspace cards, and standalone Recent/Favorites/Pinned/Trash lists. Project, collection, All Library, and workspace material can now be narrowed without leaving its current context. Multi-term filtering matches across the complete bounded Item record—including title, decoded URL, notes, tags, placements, nested metadata/status/category values, and human project/collection names—while cached normalized indexes keep keystrokes local and fast. The Library page reuses its existing filter instead of showing a duplicate and now uses the same expanded index. Quick-filter controls reflow to their own row in narrow list panes. Forty-two focused list/Home/Project/Library/responsive tests, TypeScript, `git diff --check`, and the production build pass. Optional scoped semantic/hybrid filtering is explicitly tracked in `docs/backlog.md` under V2-C with cost, embedding-coverage, latency, and deterministic-fallback constraints; this checkpoint is committed as `Improve scoped list filtering`.
- Summary: Audited the current Homebase product goals, dashboard/side-panel architecture, page routing, existing IDE-shell redesign notes, design tokens, reusable primitives, and current styling patterns. Created `design/ui-redesign` from the latest local `rename/homebase` commit. Replaced Home's automatic landing/work-tab vertical split with a full-canvas workspace model and then clarified its hierarchy: Overview/Search and project/collection quick switching now remain in a persistent Home-context header, while movable/closeable item, list, and promoted-search tabs live in a separate `Open work` layer below it. Added persistent Overview/Search sections inside Home, routed the hero search and command-palette search into Home Search, preserved search state while switching sections, retained “Open in tab” promotion, and removed the duplicate Search entry from the left Tools navigation. Separated Home Search state from promoted working-search state: promoting `python` now snapshots its query, filters, search mode, and project/collection scope into an independent tab, so later Home searches or scope changes cannot mutate it; multiple promoted searches can coexist, restore their own snapshots when activated, and drive the Inspector from the active search tab rather than Home Search. Added a Home Overview card showing the six most recent persisted library searches; selecting one reopens a fresh hybrid Home Search in the current scope without mutating promoted search tabs. Added shared Clear actions to that card and every Search surface; clearing deletes the persisted browser-storage history and immediately synchronizes Home and working-search state. Added the first scope-aware Home iteration: global Home presents project launchers with item/collection counts; selecting a project changes Home context rather than opening a tab; project Home presents its collections; collection selection narrows the same Home; favorites, recent items, totals, and Home Search use the active scope. Replaced the initial breadcrumb with a compact persisted quick-switch row: All Library remains fixed, up to five most-recently used projects stay available for back-and-forth switching, and each project remembers up to five recent collections shown as a subordinate switcher group when that project is active. New working tabs now record their temporary origin project and optional collection without changing permanent item membership: All Library tabs are global, project tabs are visible across every collection in that project, a globe control can expose a project tab everywhere, and a Current/All control reveals or hides other projects' tabs with project labels in the all-tabs view. Legacy saved tabs remain global. Restored scrolling across all opened working tabs by bounding the shared tab workspace to its parent and making the Home content host a real flex container; bookmark details can now grow inside the shared scroll shell and long note content scrolls within its full-height editor.
- Commits/PRs: `c092dea` (`Redesign Home workspace tabs and search`); `81b62b1` (`Add project and collection Home scopes`); `6d88da7` (`Redesign scoped tabs and project workspace`); `ac30083` (`Redesign project workspace sessions and focus`); `2a2976a` (`Speed up shared dashboard startup`); `5274078` (`Connect project workspace search and inspector`); `cdb2117` (`Align Home library and workspace flows`); `Simplify Home and establish Inbox baseline`; `57aed1d` (`Enforce Inbox collection rules`); `8266e16` (`Unify Home and library workspace views`); `a1e6f29` (`Align Tab Commander page shell`); `6d06352` (`Unify dashboard design and accelerate startup`); `72b7451` (`Clarify Home project and material navigation`); `e89c443` (`Refine Home layout and responsive inspector`); `Refine All Library hierarchy and project browsing`; `84ca0d7` (`Improve gallery cards and compact item details`); and `Improve scoped list filtering` on `design/ui-redesign`.
- Status: The Home hierarchy, scoped Open work, stable project switcher, project Browse workspace, Focus mode, accessibility correction, and direct-link behavior are committed. Dashboard startup now has a compact, versioned projection persisted in `chrome.storage.local`; it contains exact active-project/collection/workspace scope and stripped item summaries rather than full notes, tags, placements, and enrichment metadata. The single offscreen DB owner also serves and revision-caches this projection using a lean SQLite query, so a second dashboard in the same Chrome profile/window set reuses already-prepared startup data. Home publishes persisted or shared projection content immediately while the complete library hydrates and reconciles in the background. The DB owner announces readiness as soon as OPFS is live, then performs backup-folder reconciliation at low priority after a short delay. Home pipeline statistics and pending classification-signal maintenance wait for the complete library. Console timing markers identify persisted and shared startup-projection latency. An explicit owner/worker protocol handshake now detects an offscreen document left alive from an older development build and recreates it before forwarding RPCs, preventing `Unknown RPC method: getDashboardStartupProjection` version skew. Project Browse/current-session selection now feeds the right Inspector without forcing tab navigation. Scoped Search shows a compact active-workspace card; adding a search visibly selects its workspace entry, avoids exact snapshot duplicates, and saved search entries restore their frozen query/filter/mode in place. Inbox is now enforced as a single-collection capture surface: DB creation paths reject additional Inbox collections, and collection-creation controls are removed from Inbox across dashboard, item, side-panel, modal, and legacy views. Project pages can now save the current mixed working set as a named Homebase workspace, switch among live, named, and browser-imported workspaces, auto-preserve the workspace being left, delete named workspaces safely, and open URL-capable entries in a browser window. Project material can be added directly to any Live or named workspace even when already present elsewhere; active workspace entries expose explicit accessible Copy/Move controls, deduplicate only within the destination, and protect temporarily included global entries from accidental project-scope moves. The redundant Live-session selector stays hidden until another workspace exists. The production build and focused tests pass. The repository-wide test command still includes pre-existing standalone `.test.ts` scripts that Vitest reports as empty suites plus one unrelated categorization assertion failure.
- Next steps: Reload and review the clean three-zone Home: Find, Continue working, and Browse library. The scattered project, quick-access, recent-search, processing, and analytics cards are consolidated; Projects/Recent/Favorites share one browser, while Trash and Processing are secondary utilities and recent queries live in Search. The clean-install data model now presents `Inbox / Incoming` for unscoped capture and `Unfiled` inside normal projects. Because the product is pre-release, no legacy migration is planned: after this build is ready, uninstall/reinstall with a new backup folder and begin realistic daily-use testing. Then refine the visual system and carry the proven interaction patterns to other pages. Separately, compare startup timing markers for first open, a second dashboard, and a cold Chrome restart. The Search Dev manual embedding backfill now obtains a capped worker-side list of pending item IDs and passes that explicit scope to the embed pipeline; the unsafe unscoped guard remains intact. The DB owner protocol is now version 3 so reloads replace an older offscreen worker before invoking the new RPC. Project Pinned, Collections, and saved-workspace switching now use bounded vertical browsers instead of hidden horizontal strips. Home Projects/Recent and standalone quick-access pages were audited and already had scalable vertical browsing; Home's misleading eight-item Favorites preview is now an uncapped, vertically scrollable `Favorites & pins` browser. Generic import/digest retries no longer open temporary background Chrome tabs after a headless fetch miss, eliminating third-party preload console noise; matching existing tabs may still be reused, and explicit browser-session retrieval plus known session-required sources retain the ephemeral-tab fallback. Global Favorite is now available for both links and notes from project browse rows, project Item, All Library Item, and the existing Focus/detail pages, while project Pin remains a separate project-local action. The former Preview pane is now an editable Item workspace: title, URL, notes, Favorite, tags, and multi-project/collection membership can be changed in place using the full library catalog; Focus remains as a larger canvas rather than a capability boundary. A successful backup-folder atomic-move fallback is now a quiet debug event rather than a warning; genuine move-plus-copy failures retain both precise DOMException details instead of `[object DOMException]`.

## 2026-08-08 - Plan UI redesign (cross-agent history check)
- Session ID: `019f6b7e-5590-7600-9446-7f25d0454929`
- Agent: codex
- Resume: `codex resume 019f6b7e-5590-7600-9446-7f25d0454929`
- Summary: Defined V3 as a trust/usability release gate and added the managed `docs/V3_RELEASE_ISSUES.md` checklist/ledger. Logged V3-001 and replaced stale Help v2 with a comprehensive searchable, media-ready text/icon guide. Logged V3-002 after a completed 445-link import left a false `443 processed / 2 remaining` checkpoint; fixed scoped-run accounting so terminal skips/failures finish the run, old no-error leftovers self-heal, and Import Studio reports only checkpoint-final IDs. Recorded a post-V3 review-first organization assistant. Logged V3-003 after organization became unpredictable during parallel processing; replaced stale cross-realm whole-item writes with canonical worker-side atomic patches, made interactive updates explicitly high priority, removed the Project path's blocking full refresh, and added honest durable-save feedback/dialog locking. Logged V3-004 and added pre-ranking Search exclusions for material already in the current project or collection; corrected the first pass so the filter is contextual rather than a global organization list, reruns immediately, and clears when its context changes. Logged and implemented V3-005: Search now has parsed default-AND/phrase/OR/AND/plus/exclusion/site semantics, visible interpretation and fallback state, exact-versus-related separation, and protocol-v6 worker-side scoring against full document vectors without hydrating them into the dashboard.
- Commits/PRs: `Start V3 release validation and fix pipeline checkpoints`
- Status: V3-001 through V3-005 are READY TO RETEST. Production build passes; the latest 44 focused parsed/hybrid Search and DB-priority tests pass, alongside the earlier organization/concurrency, Help, storage, and Import/pipeline suites. The V3-003/V3-004/V3-005 fixes and post-V3 organization-assistant backlog entry are uncommitted.
- Next steps: Reload protocol-v6 extension bundles and run the V3-005 exact/semantic matrix (`ml in trading`, quoted phrase, OR, AND/plus, exclusion, site, API-key fallback, and digest coexistence). Also test organization from Search, Project, and side panel while a multi-item pipeline is active; confirm `Organization saved`, both destinations after reload, and no later reversion. In project Search widened to All Library, select `Not in <project>`, add a result, rerun, and confirm it disappears; repeat for a collection. Confirm the `443/445` banner disappears, then review Help responsively and continue Run 01 through G0–G6.

## 2026-08-09 - Stabilize V3 content storage and sleep recovery
- Session ID: `019f6b7e-5590-7600-9446-7f25d0454929`
- Agent: codex
- Resume: `codex resume 019f6b7e-5590-7600-9446-7f25d0454929`
- Summary: Audited parsed-query handling and confirmed that `trading ai` and `trading +ai` both create one combined `trading ai` query embedding; `+` is redundant because unadorned terms already use AND semantics. Confirmed material retrieval currently stores one title-plus-AI-summary vector per item; full fetched bodies are not embedded or chunked, so this is document discovery rather than full RAG. Logged V3-006 and fixed restored Search scope. Designed and implemented V3-007: fetched bodies are compressed in worker-owned `workbench-content.sqlite`, read on demand, and mirrored as one coarse snapshot. Replaced the first V3-008 recovery attempt with the documented durable coordinator architecture: SQLite schema v5 now owns `pipeline_jobs`/`pipeline_tasks`, checkpoints, priorities, leases, and outcomes; the offscreen document is the sole full-bulk and interactive-single execution authority; bulk is serialized while singles run in an independent high-priority lane. Jobs commit before acknowledgement, expired leases recover automatically at coordinator startup or the one-minute Chrome wake alarm, and Resume requeues without closing the offscreen document or DB worker. Cancel/Pause are distinct durable transitions, folder permission is absent from runtime recovery, checkpoints are deleted by exact job ID, and every dashboard reads shared SQLite status plus live coordinator progress. Enrichment retains its truthful two-second active/started/elapsed heartbeat. The side-panel Dashboard action remains non-blocking.
- Commits/PRs: `Harden organization and parsed hybrid search`
- Status: V3-003 through V3-008 are ready for extension retesting. The first coordinator dogfood run exposed a 5% submission stall and an indefinitely loading Enrichment Hub: the dashboard built its full seed before durable acceptance using per-item DB queries, monopolizing the single SQLite worker. Submission now sends only IDs/options, queue rows commit first, seed construction happens in the coordinator with chunked bulk reads, and 445 task rows are checkpointed in five statements. Hub page/count RPCs additionally time out after 15 seconds and show an explicit Retry state rather than an endless loader while retaining cached rows. A later Cancel/re-import test exposed the final divergent runner: Import Studio still executed its pipeline inside the dashboard tab and its save path waited on a full cache hydrate. Import Studio now submits through the shared offscreen coordinator, import save refreshes only the affected Items (in bounded chunks) plus Trash after the atomic worker transaction, and a returned paused/failed checkpoint can no longer be mislabeled completed. The next one-link Hub retest found three concrete feedback/blocking bugs: two-second heartbeats invalidated durable UI state, offscreen stage-write broadcasts caused full peer-dashboard reloads, and every pipeline synchronously exported the whole content sidecar to the selected/Dropbox folder before finishing; Cancel also failed to reach embedding. Heartbeats now carry progress only, peer dashboards ignore intermediate derived-data writes, sidecar mirroring stays scheduled outside job completion, and Abort propagates through embedding while cancellation status is durably acknowledged. Protocol is v12 and SQLite schema is v5. The production build, TypeScript, `git diff --check`, and 30 focused Hub/import/client/coordinator/queue/cache tests pass; actual Chrome inspection was unavailable because no browser target was attached. Changes after `33396e1` remain uncommitted.
- Next steps: Reload protocol-v12 `dist` so Chrome replaces the stuck owner. In Enrichment Hub, process one link and confirm the page remains stable and the job completes; repeat and Cancel during fetch/embedding. Only then test a small multi-link run, dashboard close/reopen, sleep/wake recovery, and the 445-link batch.

## 2026-08-09 - Analyze V3 pipeline architecture
- Session ID: `019fe90a-c0e4-7740-a55f-bf421fcb4953`
- Agent: codex
- Resume: `codex resume 019fe90a-c0e4-7740-a55f-bf421fcb4953`
- Summary: Audited and safely separated the failed coordinator/content attempt, accepted folder-only `workbench-content.sqlite`, and then replaced the incremental compatibility approach with a clean coordinator rebuild. One generic durable offscreen engine now owns one/many URL processing plus re-extract, re-embed, classify, discovery, review/debug controls, backfill, and Import Studio; pages only submit/observe/cancel. Removed the old item/batch/scoped/import runners, checkpoint Resume UI, cross-tab execution lock, independent lanes, and automatic run artifacts. Added item-ordered durable stages, cross-tab status/cancel observation, suppressed intermediate refresh broadcasts with one scoped completion event, and alarm/browser-start recovery that continues later batch items after an interrupted paid stage. Post-test inspection of the active OPFS database confirmed the coordinator is progressing; all 11 recent successful fetches had AI `not_configured`, embedding skipped as not AI-ready, and classification failed with a missing-API-key error rather than a pipeline stall. The saved 73-character key and successful Settings test disproved missing configuration: the clean client discarded its supported `aiSettings` field and the host never installed the existing override. The client now sends saved settings ephemerally, the serialized host installs/clears them per job, recovery reloads storage, and durable payloads continue to exclude secrets. On 2026-08-10, local JSONL evidence showed the Codex client submitted an older mid-turn/steered message again 1h40m later as a distinct user event with new message, turn, and client IDs, immediately after another task completed; this is consistent with stale queued-input replay at a task boundary, not context compaction or a pipeline-direction change.
- Commits/PRs: `33ee5f5` architecture; `cfad800` V3-006 fix; `7809529` V3-006 acceptance; `e9aef10` folder-only content store; `a7acc31` durable pipeline job primitives; `971faf7` one-link coordinator; clean replacement `2f9c4d0`; AI-settings handoff `5a4893a`; priority scheduler `a727f8c`; authenticated browser fetching `e2b84fa`/`0190dfb`; dashboard-window binding `6b503c3`; safety snapshot `a2816da` on `rescue-v3-content-coordinator-attempt-2026-08-09`.
- Status: Clean rebuild is committed as `2f9c4d0`; the AI-settings handoff fix is committed as `5a4893a`. Protocol v13 cooperative priority scheduling is committed as `a727f8c`, protocol-v14 service-worker browser fetching as `e2b84fa`, and ordinary-URL browser-first routing as `0190dfb`. The schema-v6 window-affinity follow-up captures the trusted sender tab/window at Start and Resume, scopes matching/temporary tabs to that window, requests a cooperative stage-boundary pause when the owner dashboard closes, exposes Resume from every dashboard, and rebinds unfinished work to the Resume caller's window. Browser restart without a live session binding pauses rather than guessing. The core/content protocol responses are synchronized at v14 so owner validation no longer recreates a healthy offscreen document. Live inspection confirmed the new job persisted real owner/window IDs and every audited fetch used `tab-session`. Review found seven post-Resume classification failures were all `Missing API key`, not fetch/window failures: Resume had restored durable options without the intentionally non-durable AI settings. Resume now resends settings ephemerally from its dashboard and still omits the key from SQLite. Twenty-nine focused job-store/client/browser-routing tests, TypeScript, syntax checks, `git diff --check`, and the production build pass.
- Next steps: After returning, reload commit `6b503c3` and run a five-link window-affinity test from dashboard window A: focus B, verify tabs remain in A, close A's dashboard, wait for Paused, Resume in B, and confirm both `tab-session` and successful post-Resume AI/classification. Then test cancellation during tab load, navigation/refresh, sleep/wake with the owner dashboard open, Chrome-restart pause/Resume, and final large-batch accounting. Taxonomy discovery remains separate. End-of-chat handoff was reviewed on 2026-08-10; no pipeline implementation work remains uncommitted.
- Return update — 2026-08-12: User returned to resume real-extension testing. The worker/content-store implementation remains at committed checkpoint `6b503c3`; acceptance testing, rather than more architectural implementation, is next.

## 2026-08-12 - Restore pipeline Resume progress and results
- Session ID: `019fe90a-c0e4-7740-a55f-bf421fcb4953`
- Agent: codex
- Resume: `codex resume 019fe90a-c0e4-7740-a55f-bf421fcb4953`
- Summary: Fixed the Resume presentation gap without changing pipeline ownership. Resume now observes the existing durable coordinator job through the shared progress modal, starts at the saved completed count, supports cancellation, and shows the same final summary/report UI as a normal Start.
- Status: Implemented and manually accepted as mostly passing; focused Resume client/UI tests and production build pass. Broader lifecycle behavior remains under general V3 dogfood.
- Next steps: Commit this checkpoint, then return to the V3 release checklist with emphasis on data durability/reinstall recovery, core workflows, responsive UI/UX, state retention, and bounded pipeline lifecycle checks.
- Follow-up: Confirmed `docs/V3_RELEASE_ISSUES.md` as the persistent operating document for all V3 dogfood. Added the current `4506de7` checkpoint, explicit cross-session tracking workflow, corrected V3-003–005 commit references, and a 2026-08-12 Run 01 continuation entry so issue-fix detours cannot replace the release gates or A–G checklist.
- V3-009 follow-up: Fixed misleading large-job progress bars. Stage-local `1/1` callbacks now remain separate from authoritative whole-job item counts, and all pipeline progress consumers use one shared calculation. At `540/3000`, every stage renders about 18% instead of jumping near completion. Twenty-three focused tests and the production build pass; manual extension retest is pending.
- V3-010 follow-up: Audited repeat-import merge semantics and found same-destination placement notes/tags plus longer imported titles could replace existing values, while every affected bookmark was selected for forced AI processing. The uncommitted fix preserves meaningful titles, idempotently combines distinct placement notes, unions placement tags, keeps organization additive, and defaults Import Step 3 to new/missing/incomplete enrichment. A scoped protocol-v15 worker query avoids whole-pipeline hydration; optional 7/30/90-day and reprocess-all policies remain explicit. Twelve focused tests and the production build pass; the manual repeat-import preservation matrix is pending.
- V3-011 diagnosis: Verified Import uses the shared durable `full_digest` path and the service worker binds the caller's dashboard tab/window. Ordinary URLs request an inactive authenticated tab first in that window. The current 4,482-item job contains only X/Twitter URLs, whose deliberate `x_syndication_first` route explains the absence of opened tabs: 843 debug records skipped the quick tab path, with 813 final `syndication-expanded` and 30 `local` sources. Separately, the sleep/wake run is not healthy: AI was `ok` through 17:44 but predominantly `not_configured` from 17:45 onward, leaving hundreds of embed/classify/finalize tasks skipped or failed. Logged V3-011; current batch should be cancelled before focused recovery/settings diagnosis.
- V3-012 diagnosis: Public X capture is substantive but bounded: the live DB has 1,700 successful strict X syndication rows and every one has the matching raw content document; 829 show multi-part thread/conversation structure, 1,587 index references, and 1,106 include followed linked bodies. The provider captures the root-author self-thread plus ancestor path to the bookmark (20-hop cap), expands at most two quoted threads, and follows at most three external links at 10k characters each; it does not capture every reply branch. Protected/private X is a real gap: 47 strict X rows currently fail, X does not open an ephemeral authenticated fallback, single-post output from an already-open matching X tab is rejected by the multi-part acceptance rule, and authenticated linked pages are not tab-fetched. Logged V3-012 for a syndication-first plus authenticated-fallback design.
- V3-012 implementation: Kept rich public X syndication first, added dashboard-window-bound authenticated tab fallback for unavailable/private X, accepted substantive known-author single posts, and upgraded the tab extractor to retain handles, links, media, and up to 20 rendered conversation cards over four bounded scroll passes. Thin X captures now follow up to three external links through background tabs in the same bound window before public fallback. Twenty focused tests and the production build pass; manual Chrome acceptance remains, and the separate V3-011 sleep/settings failure is unchanged.
- V3-013 implementation — 2026-08-13: Fixed cross-surface Inspector selection. Library/Notes selection now supplies the shared right Inspector even when a stale Home item tab exists. Enrichment Hub's “Open in Inspector sidebar” now opens the item tab, selects/expands Inspector, and navigates from the full-width Hub to Home where that sidebar is visible. Fifteen focused shell/Library tests and the production build pass; manual extension retest remains.
- V3-013 design correction — 2026-08-14: User testing exposed three remaining semantic issues: restored Library selection does not initialize the shell Inspector before a new click, workspace-tab actions only make sense on Home, and navigating Enrichment Hub to Home is disruptive. No product code changed pending agreement. Recommended ownership: one shell-level selected item for Home/Library/Search; workspace tabs and tab actions only on Home; Enrichment Hub keeps its purpose-built local inspector and never navigates automatically, with at most an explicitly labeled “Edit in Library” escape action.
- V3-013 focused fix — 2026-08-14: Implemented only the agreed first and third corrections. The shell now restores the persisted Library/Notes selected ID before child hydration, does not erase it while the canonical library is still loading, directly resolves a missing selected row, and renders a truthful loading state instead of the false empty Inspector. Enrichment Hub no longer navigates to Home. Workspace-tab actions/labels were deliberately left unchanged for a separate decision. Seventeen focused tests and the production build pass; manual extension retest remains.
- V3-013 Hub Inspector follow-up — 2026-08-14: Made Enrichment Hub a full-middle surface with the shared right Inspector rendered beside it. The Hub's Inspector action now sets an inspect-only item selection, expands/selects the shared Inspector, and remains on the Hub without creating or activating a Home workspace tab. The wider workspace-tab design remains deferred. Twenty focused Hub/Library/shell tests, the production build, and `git diff --check` pass; manual extension layout/behavior retest remains.
- Checkpoint commit prepared — 2026-08-14: The accumulated reviewed V3 work for repeat-import preservation/processing policy, authenticated and thin-link X fetching, and cross-surface Inspector behavior is being committed together as `Stabilize V3 imports, X fetching, and inspectors`; unrelated `.kilo/` remains excluded. Workspace-tab ownership/design is the next discussion and no additional tab changes are included.
- Workspace-tab design discussion — 2026-08-14: Committed the checkpoint as `674ee41`. Clarified the recommended hybrid model: Home remains the complete workspace/tab canvas; Library can switch its left source between All library and the shared open-workspace subset while preserving its item workspace and Inspector; task pages such as Enrichment expose the same working set compactly rather than duplicating a full tab canvas. Created an interactive Library wireframe for the decision; no product tab behavior changed.
- Workspace-tab architecture correction — 2026-08-14: A code audit confirmed the current model is internally contradictory: one persisted tab array is partitioned into global/project scopes and named workspace snapshots, while Home hides its ordinary canvas and overlays a separate full-screen `GlobalTabSystem` whenever `activeTabId` is set. Adding, selecting, and focusing workspace entries follow different paths; ordinary Home exposes no persistent tab strip, and the focus return control is small or absent when the tab sidebar is collapsed. No product code changed. Recommended replacing project-owned invisible tab universes with one application-global Open Work set whose entries retain project/collection context; named workspaces become explicit saved sets, and Focus becomes an optional presentation state rather than normal tab navigation.
- Tab/workspace terminology decision — 2026-08-14: Proposed one application-global Open Work tab system (multiple open tabs, never silently partitioned by project). A tab is transient UI state referencing an item/search/list; closing it never deletes or reorganizes data. A workspace is a deliberately named, durable saved set that can repopulate/merge into Open Work and may carry project context without owning or hiding tabs. Project/collection membership remains permanent organization, while browser-window captures should be called browser snapshots/sessions rather than another workspace type. Product code remains unchanged pending user agreement.
- Tab/workspace simplification follow-up — 2026-08-14: Refined the proposal: tabs need not be a separate user-facing data concept. The application always has one global, persisted Current workspace/Open Work set; its entries are presented as tabs for quick switching. Named workspaces are additional saved sets of the same entry type. “Open” adds/selects an entry in Current workspace, while “Add to workspace” saves it to a chosen named set without forcing navigation. This preserves lightweight ad-hoc opening and avoids requiring every inspected item to be deliberately filed. No product code changed.
- Active-workspace terminology correction — 2026-08-14: Rejected “current workspace” versus “saved workspace” as false workspace types. Proposed that every workspace persist automatically: one built-in General workspace plus user-named workspaces, with exactly one active workspace whose entries occupy the single global tab strip. Opening/switching a workspace makes it active; ordinary Open adds to the active workspace, while Add to workspace can target another without switching. Active workspace and active tab are separate states. Projects do not own workspaces or tabs. No product code changed.
- Workspace hierarchy agreement — 2026-08-14: User selected one shared Global workspace, one automatically created General workspace per project, and user-named workspaces within each project. Links/items can be opened or added into any destination from Inspector, Search, detail pages, and other surfaces; workspace references must support obvious copy/move/remove operations. Guardrail: project navigation must not silently activate its General workspace; activation is explicit, while project context may reorder/suggest destinations. Workspace movement changes only workspace membership, not the canonical item or its permanent project/collection organization. No product code changed.
- Workspace-model foundation — 2026-08-14: Documented the accepted model as canonical and implemented one explicit app-level active workspace, deterministic Global/project-General identities, project-owned named workspaces, per-workspace entry/focus restoration, reference-only copy/move/add/remove semantics, and a one-time migration from the former project-scoped tab state. Project/page navigation no longer filters or switches open work; generic Open now targets the explicitly active workspace. Browser snapshots are excluded from ordinary workspace destinations and remain a separate capture type. Replaced active `Open in tab`/Live-session product language with workspace terminology while retaining tab-strip/ARIA concepts only as presentation compatibility. Fixed reload leakage that could copy a named workspace into project General. Sixty-nine focused tests, a final 30-test state regression pass, TypeScript, `git diff --check`, and the production build pass. The fundamental Home-versus-task-page workspace presentation remains deliberately deferred for the next UX review; changes are uncommitted.
- Internal-tab UX removal — 2026-08-14: Removed the internal tab presentation from active Homebase paths while preserving external Chrome/browser-tab features. Workspace entries now use entry-list/pill controls and Add/Open/Remove language; Add does not navigate, while Open adds if needed and explicitly activates the destination and item. The shared picker shows `In N workspaces`, per-destination Added state, and separate Open actions across Home, Library, Search, Project, and Inspector. Workspace labels are project-scoped (`Project — General`, `Project — Name`), and legacy Collections/Projects tab canvases are no longer active UI routes. Eighty-two focused tests plus a final 40-test regression subset, TypeScript, `git diff --check`, and the production build pass; V3-014 is ready for manual extension retest and remains uncommitted.
- Workspace-action design reconsideration — 2026-08-14: User found `Open in workspace` ambiguous and disruptive. Proposed making `Add to workspace…` the sole item-level entry action: it always opens the destination picker, adding never navigates or changes the active view, and an explicit secondary `View workspace` action is available after selection. Focus mode is under reconsideration; no code changed pending agreement on normal selected-item versus dedicated focus presentation.
- Workspace Focus recommendation — 2026-08-14: Recommended removing Focus as a workspace navigation/state concept for V3. A workspace is already the focused subset; `View workspace` should open its normal list/detail page, while any future distraction-free presentation should be an optional reversible layout toggle inside that page, never triggered by Add, selection, or workspace activation. No code changed.
- Quiet Add and no-Focus implementation — 2026-08-14: Replaced item-level `Open in workspace` with a single membership-aware `Add to workspace…` picker. Add now preserves the current page, active workspace, and selection; each destination exposes a separate explicit `View workspace` action that adds if needed, activates the named destination, navigates to Home, and selects the item in the ordinary workspace list/detail canvas. Removed Home's full-canvas Focus takeover and its buttons/return branches, made search/list additions quiet, and updated Library, Search, Project, All Library, Inspector, Workspaces, Help, canonical design, and V3-014 tracking. External Chrome tabs are unchanged. Twelve focused suites/82 tests, TypeScript, `git diff --check`, and the production build pass; manual extension acceptance is next and changes remain uncommitted.
- Workspace Add safety/UI correction — 2026-08-14: User testing found the Add control nonfunctional/ambiguous and the implicit active-workspace shortcuts unsafe. The shared item/search/list action now always opens a bounded destination modal, preselects (but never automatically writes to) the active workspace, requires an explicit footer confirmation, shows selected/added state, and keeps `View workspace` separate. Removed remaining direct-active and misleading fallback controls, hardened compact text layout, and updated Help/design/V3-014. Twelve focused suites/82 tests, TypeScript, `git diff --check`, and the production build pass; live screenshot QA remains unavailable because no browser surface is attached. Changes remain uncommitted for manual extension retest.
- Similar-links correction and latency diagnosis — 2026-08-14: Restored the Inspector's original Similar bookmarks presentation and replaced only its former `Open tab` action with the same confirmation-based `Add to workspace…` picker. Removed the mistakenly introduced Inspect action; Similar Add neither changes selection nor navigates. Thirteen focused suites/83 tests and the production build pass. Investigation found the Similar spinner rebuilds a full six-table page-side search index for every selected item and may then make a futile 60-second embedding request because vectors intentionally remain worker-owned. Recommended a separate worker-owned `find similar by item ID` query returning only top result metadata; no performance architecture was changed yet.
- Similar retrieval fix — 2026-08-14: Implemented the requested retrieval-first correction without changing the reported broken row layout. Similar ranking now runs in the DB worker where vectors actually live, caches its search index by database revision, and returns only result metadata; the page no longer rebuilds six full tables or makes the futile 60-second embedding fallback. Added title/tag/domain fallback for items without vectors/categories so sparse links still receive plausible local candidates. Fourteen focused suites/85 tests, TypeScript, `git diff --check`, and the production build pass. Manual extension retest of retrieval latency/coverage is next; row layout remains the following task.
- Similar performance review — 2026-08-14: User retest showed worker retrieval still takes several seconds; no code changed. History confirms the former fast path used already-hydrated in-tab arrays before `4f74039` moved vectors worker-only for OOM safety. The new RPC rebuilds items/enrichments/signals/links/categories/collections from SQLite whenever the global DB revision changes, converts every vector BLOB to a fresh JS number array, and runs the new full-library lexical fallback even for embedded anchors. A 4,500×1,536 synthetic review measured current warm query at 147–178 ms versus 9.5 ms for cosine+sort alone and 235 ms just for BLOB-to-number-array conversion, excluding SQLite/index construction. Correct design: one prewarmed worker-only typed-vector/metadata index, search-specific incremental invalidation, vector-only primary queries, and a pretokenized fallback used only when the anchor lacks a vector. Current retrieval patch should not be considered accepted; UI layout remains deferred.
- Similar rollback decision — 2026-08-14: User clarified that Similar was already good enough and the workspace task must not redesign unrelated retrieval. Recommended selectively removing only the unaccepted worker full-index RPC and lexical-ranking changes, restoring the pre-task Similar retrieval behavior exactly, while retaining only the requested replacement of `Open tab` with the shared non-navigating `Add to workspace…` modal. Any future worker-only typed similarity index must be a separate measured task, not part of this workspace fix. No product code changed in this turn.
- Similar historical clarification — 2026-08-14: No modernization is required. The original semantically fast version was fast because every dashboard tab held the full embedding arrays in hydrated memory; `4f74039` removed those duplicate tab copies to stop large-library/digest OOM, but Similar was not adapted. The later pre-workspace version remained fast for category/tag matches but lacked worker vectors and could enter a 60-second network embedding fallback for sparse items. The minimal repair is therefore to restore that existing hot metadata path and bridge only vector ranking to the worker; restoring full vectors to tabs would reintroduce the proven memory problem. No product code changed.
- Long-term Similar index design — 2026-08-14: User prefers a durable solution rather than another temporary cache. Recommended precomputing a small versioned top-neighbor edge set for each saved item and serving Inspector Similar via one indexed lookup, rather than loading/scanning vectors on interaction. Embeddings remain canonical worker-owned BLOBs; a disposable derived similarity index stores only anchor/neighbor IDs, rank, score, embedding model/text hash, and scoring-policy version. Pipeline completion only enqueues index maintenance—never blocks on it. New/changed items update their own and affected reverse top-K edges; import/model/policy changes trigger a durable background rebuild. Missing/stale rows return immediate category/tag fallback and schedule repair, never a network call or indefinite spinner. Arbitrary free-text semantic search remains a separate worker-vector problem. No product code changed.
- Similar storage correction — 2026-08-14: User rejected a second derived database as unnecessary complexity. Corrected design: use only the existing main SQLite database and existing DB worker. The simplest representation is a small versioned precomputed-neighbor payload on each existing `ai_item_signals` row (or, only if relational querying proves necessary, one table in the same DB—not another file/worker). Inspector performs one keyed item-ID read and joins the returned neighbor IDs against already-hydrated item metadata. No second database, no second owner, no new synchronization or backup lifecycle. No product code changed.
- Similar live-index correction — 2026-08-14: User correctly rejected persisted top-neighbor lists because a new vector makes frozen neighbor snapshots stale and creates reverse recomputation work. Final recommended direction: the existing DB remains the sole durable source of embedding BLOBs, while the existing DB worker owns one live normalized `Float32Array` vector index plus lightweight item metadata. It warms once after DB open, appends/updates/deletes one vector incrementally on signal mutations, and answers Similar by anchor lookup plus current top-K dot-product scan; a new link requires no recomputation of old neighbor lists and is automatically considered by every later query. No vectors return to dashboard tabs, no second DB/worker exists, and no global-revision rebuild is allowed. At the current ~4.5k scale the measured full vector scan is ~9.5 ms; an ANN implementation can later replace the internal scan behind the same API only if scale requires it. No product code changed.
- Similar cache terminology — 2026-08-14: Confirmed the problem is inherently dynamic and should use a fast in-memory index, not another database. SQLite remains the single durable source; the existing DB worker materializes one process-local normalized vector matrix and item-ID/metadata maps, updates them transactionally after successful signal mutations, and rebuilds once after worker restart. The cache is disposable, non-backed-up, and never copied to dashboard tabs. No product code changed.
- Similar index sizing — 2026-08-14: At 20,000 items × 1,536 dimensions, one contiguous Float32 matrix is ~122.9 MB decimal / 117.2 MiB, plus a small ID map/norm array. This is feasible as one DB-worker copy but not as JS `number[]` objects or duplicated dashboard-tab caches. The index must be built directly from embedding BLOBs into a compact matrix with temporary allocations released. An exact scan is ~30.7M multiply-adds and may be tens of milliseconds; define a stable `upsert/remove/query` index boundary now so a WASM-SIMD or ANN implementation can replace the flat scan at larger scale without changing storage/UI architecture. No product code changed.
- Similar live-index implementation — 2026-08-14: Replaced the unaccepted global-revision/six-table Similar rebuild with one disposable DB-worker vector index. SQLite embedding BLOBs stream in bounded pages into pre-sized normalized `Float32Array` shards, top-K uses an exact heap scan isolated by model/dimension, and successful signal/item mutations incrementally upsert/remove rows. A user retest exposed that the first version still let child Similar enqueue a cold worker metadata build before the parent Inspector context read, delaying both sections. The corrected path keeps hot/partial metadata and the inverted no-vector fallback in the dashboard, requests only vector scores from the worker at explicit low priority after a deferred tick, never blocks Inspector first paint on pipeline hydration, and performs no proactive worker warm. A synthetic 20k×1536 vector build measured ~111 ms and top-200 query ~64 ms on this machine. Twenty-one broader search/storage tests, TypeScript, `git diff --check`, and the production build pass; corrected extension latency still needs manual retest.
- Similar cache lifecycle clarification — 2026-08-14: The vector matrix is currently a shared process-memory cache owned by the DB worker. It cold-loads lazily on the first Similar request in 512-row SQLite BLOB pages, remains available to every dashboard for the worker lifetime, updates incrementally after signal writes, and is cleared only by worker loss or whole-store replace/reset. This avoids startup interference but leaves a one-time cold Similar delay; the next scale refinement, if manual retest still finds that visible, is cooperative background warming in yieldable chunks rather than a second persisted index or an uninterruptible startup load.
- Similar cooperative warm-up — 2026-08-14: User confirmed the lazy cache improved repeated loads but cold and warm Similar were still visibly slow. The DB worker now starts vector warming only after returning the essential dashboard projection, streams 256-row SQLite pages into a private candidate in 64-vector chunks, yields between chunks outside the serialized RPC pump, publishes only if no relevant mutation/store replacement invalidated the snapshot, and retries after 750 ms of write quiet. The 12-row panel now requests 48 rather than 200 scored candidates without reducing the full-library scan, and slow calls emit phase timings for metadata, RPC/warm, worker scan, and composition. Twenty-two focused tests, TypeScript, `git diff --check`, and the production build pass; extension reload and first/repeated Inspector timing remain the acceptance test.
- Library-versus-Home latency diagnosis — 2026-08-14: User reported that the same Inspector/Similar selection is fast from Home but very slow from Library and requested analysis only. Both surfaces use the same shared RightPanel/ItemSimilarSection, so vector ranking cannot explain the difference. Library uniquely submits an unchunked, default-high-priority badge request for every scoped link; the worker reads all pipeline tables and computes the entire badge map before it can service the selected item's keyed context RPC, and Similar is not mounted until that context resolves. Library also rebuilds thousands of row descriptors/search strings and ContentBrowser progressively mounts the complete list rather than a virtual window, which can block React before and after the measured Similar call. These paths came from the July `6d06352`/`8266e16` browsing/startup work (with `b1114b4` adding all-row quick-filter work), not the current vector algorithm. No product code changed; recommended next correction is visible-window/low-priority badge loading plus a truly virtualized, memoized Library list before touching similarity storage again.
- Shared Inspector-flow invariant — 2026-08-14: User clarified that Home, Library, Enrichment, Search, and every other caller must use exactly the same Inspector/Similar execution flow; surface-specific fixes are unacceptable. Agreed architecture: surfaces submit only a selected item ID to one shared Inspector controller, which owns the keyed context read, Similar query, cache/revision policy, cancellation/stale-response handling, and observable loading/result state. Page-owned bulk work such as Library badges must be chunked/low-priority and structurally unable to delay the interactive controller; list rendering must also be bounded/virtualized so it cannot delay shared result paint. No product code changed pending implementation approval.
- Shared Inspector-flow implementation — 2026-08-14: Implemented one keyed `useInspectorItemData` resource for the shared Right Inspector and Enrichment Similar surface. Every product caller now starts the same deduplicated context request first and only then the same Similar request; results are cached for up to 100 inspected items, invalidated on relevant item/pipeline changes, and guarded against stale in-flight publication. Similar no longer has an independent component-owned fetch lifecycle. Library pipeline badges no longer submit one high-priority full-library/full-pipeline-table read: they run as low-priority 80-item RPC turns, each using five bounded `IN` queries and a signal projection that excludes embedding BLOBs. Library row models are memoized and individual content rows are memoized so selection does not rebuild every row. Focused 36-test integration coverage, TypeScript, `git diff --check`, and the production build pass. The full Vitest discovery run executed 279 passing tests but retains 22 known standalone assertion files with no Vitest suite plus one unrelated pre-existing categorization assertion failure. Manual Home/Library/Enrichment parity and rapid-selection testing is next; changes remain uncommitted.
- Shared Inspector checkpoint and Similar row layout — 2026-08-14: Committed the unified workspace and shared Inspector/Similar flow as `2a54fbd` after the user confirmed retrieval was materially better. Then corrected the narrow Inspector layout without changing retrieval or actions: each Similar result now gives its title and URL icon a full-width line, keeps domain/category/score readable beneath it, and places the existing non-navigating `Add to workspace…` modal trigger on its own row. The Similar heading/match-method text and candidate count are also formatted cleanly. Seven focused UI tests, TypeScript, `git diff --check`, and the production build pass; the layout changes are included in the following Inspector UI checkpoint.
- Enrichment Detail/Inspector synchronization — 2026-08-14: After three incorrect interpretations, the exact issue was identified: selecting Detail/eye opened the Enrichment or Classification detail panel, which still contained a separate `Inspector` / `Open in Inspector sidebar` action that had to be clicked before the shared right Inspector changed. Detail selection now synchronously sends that item to the shared Inspector in both Hub lanes, and both redundant Inspector buttons were removed from `PipelineItemInspectorPanel`. Row click, individual checkbox selection, and Enrichment review Prev/Next also synchronize. Fifteen focused Hub/shell/Similar tests, TypeScript, `git diff --check`, and the production build pass. No live browser surface was available for rendered extension verification; this and the Similar layout fix are checkpointed as `Polish Similar links and sync Enrichment Inspector` for later user retest.

## 2026-08-15 - Locate latest project chat
- Session ID: `01a00813-a0c9-78f3-a168-b4ba1d5a48c0`
- Agent: codex
- Surface: VS Code
- Project path: `/Users/amir/projects/personal_tools/workbench_agent`
- Resume: `codex resume 01a00813-a0c9-78f3-a168-b4ba1d5a48c0`
- Summary: Checked repo-local and native agent histories strictly for this project. Identified the last pre-restart extension chat as `Analyze V3 pipeline architecture` (`019fe90a-c0e4-7740-a55f-bf421fcb4953`), ending with checkpoint commit `dbafa70`.
- Status: Confirmed; the user reopened the correct pre-restart project session.
- Next steps: Continue from commit `dbafa70`; manually retest the Enrichment Detail → shared Inspector synchronization and the narrow Similar-links layout before making further changes.

## 2026-08-17 - Resume V3 release wrap-up
- Session ID: `019fe90a-c0e4-7740-a55f-bf421fcb4953`
- Agent: codex
- Resume: `codex resume 019fe90a-c0e4-7740-a55f-bf421fcb4953`
- Summary: Reconstructed the committed V3 checkpoint at `dbafa70`, then aligned the release process with the user's intent: normal daily-use dogfooding and usability fixes are primary, while robustness and data retention/integrity are the deliberate gates before trusting real data. Made the shared Inspector a wider 380px default and added a persisted draggable/keyboard-accessible divider; narrow displays retain a safe cap. Updated the V3 checkpoint, V3-014/V3-015 commit references, and logged V3-016.
- Status: Inspector resizing is implemented and checkpointed for extension review. TypeScript, 25 focused shell/responsive tests, `git diff --check`, and the production build pass. The reproduced sleep/AI-settings scenario remains uncleared as a robustness gate.
- Next steps: Reload the extension and assess Inspector width/drag behavior naturally from Search and other detail-heavy surfaces. Continue usability dogfood; before importing real personal data, deliberately validate restart/reinstall/restore, interrupted writes, repeat-import preservation, and coordinator sleep/cancel recovery.
- Universal drag/drop design review: Mapped the existing list surfaces and found fragmented legacy `text/plain` drag handlers rather than one item-transfer contract. Agreed direction for discussion is one app-wide typed drag language with a strict distinction between reference/result views (Search, Similar, All Items, Enrichment: always additive), membership containers (Workspaces and Collections: additive by default, explicit Move only), and same-container reordering. A global destination tray would make cross-page targets reachable while preserving the existing click/keyboard picker. No product code was changed for this feature.
- Drag operation refinement: A result/reference source offers only Add at drop time. A real container source carries its exact source membership, so dropping into another eligible container prompts for `Copy/Add` versus `Move`; Move removes only that recorded source membership and only after the target addition succeeds. Cross-type container semantics must be labeled explicitly rather than inferred. No product code changed.
- Container-type rule: Workspace lifetime is irrelevant—a workspace may be durable. Copy versus Move is determined strictly by compatible container type: Workspace-to-Workspace and Collection/Project-to-Collection/Project offer Copy or Move; crossing between Workspace and Collection/Project is Copy only; same-container drag is reorder/no-op. A Project may appear as a UX destination but must resolve to its explicit default/General collection because project membership is collection-derived. No product code changed.
- Final hierarchy clarification: Projects group Collections; actual organizational membership transfers are always Collection-to-Collection, whether the target collection is in the same or a different project. `All Library` is a read-only aggregate/reference source like Search and therefore only adds/copies outward and is not a meaningful destination. `Global Workspace` is an ordinary Workspace for drag/drop purposes and participates in the same Workspace-to-Workspace Copy/Move and reordering behavior. No product code changed.
- Drag entity scope: Notes and URLs are the same transferable `item` for drag/drop. Content type changes presentation only, not source classification, allowed destinations, Copy/Move rules, durability, or undo behavior. The design is ready for staged full implementation after explicit go-ahead; no product code changed in this review.
- Implementation checkpoint: Committed the prior Inspector-resizing work as `d824bad`, then implemented the universal typed item drag system. All Library/Search/Similar/quick-access/Enrichment/collection views are reference or exact-container sources; a global grouped destination tray and visible Workspace/Collection drop zones share one transfer coordinator. Same-type transfers prompt Copy/Move, cross-type transfers copy, Global behaves as a normal Workspace, Workspace rows reorder, and collection moves/Undo preserve and merge placement notes/tags through the canonical atomic item update. URLs and notes share the same path. TypeScript, 73 affected UI/storage tests, `git diff --check`, and the production build pass; manual extension acceptance is pending.
- Project All-items drop follow-up: Committed the universal drag/drop baseline as `8012821`. The project aggregate now acts only as a routing target: a project with one collection resolves the drop directly, while a project with multiple collections opens an explicit chooser before the shared transfer path runs. No direct project membership or navigation was added. TypeScript, 50 affected drag/list tests, `git diff --check`, and the production build pass; manual extension acceptance remains.
- Navigation/context design review: Committed Project All-items routing as `4be518f`. Confirmed that Enrichment currently erases project/collection scope and that Search's open/inspect path can activate workspace state and switch to Home. Proposed four orthogonal UI states for discussion—current activity, organization scope, active workspace, and ephemeral preview—with a persistent changeable workspace/context control and a non-navigating resizable preview dock. No product code changed after the commit; design agreement is pending.
- Preview design refinement: A right-dock Preview would compete with Similar/Suggestions, so the preferred direction is one app-wide in-app Peek overlay. Opening it preserves the underlying activity, query, filters, scroll, selection, Inspector suggestions, scope, and workspace; closing returns exactly to that state. The overlay can provide stored-content preview, previous/next result navigation, explicit external-browser opening, and inline collection/workspace organization without creating internal tabs or silently navigating. Avoid a separate browser popup and avoid nested destination modals. No product code changed; interaction details remain to be agreed.
- Proposed implementation stages from the context/Peek discussion: (1) separate activity, organization scope, active workspace, and preview state; preserve project/collection scope across Search and Enrichment and add a non-navigating active-workspace chooser with an explicit View action; (2) standardize item interactions so selection only updates Inspector, Preview opens one app-wide overlay, and Open original alone launches Chrome; (3) build the overlay around stored normalized content with previous/next result navigation and inline organization controls using the existing Copy/Move rules; (4) migrate every item surface and remove the legacy Search-to-Home/workspace activation path; (5) manually verify state preservation and organization integrity across all surfaces. No product code changed.
- Context/Peek implementation checkpoint: Kept project/collection scope when opening Enrichment, exposed grouped collections at All Library scope, and added a persistent active-workspace chooser whose selection does not navigate and whose View action is explicit. Removed Search's hidden workspace-entry creation/Home redirect. Added one dashboard-level Peek overlay backed by the stored content sidecar, ordered Previous/Next, explicit Chrome opening, canonical collection/tag editing, and explicit workspace Add/View controls. Shared browse, Search, Similar, quick-access, Notes, Project/collection, and Enrichment surfaces now open this same overlay; legacy navigational rows use a dedicated Preview control so opening Peek cannot first replace the underlying view. Product docs and V3-018 track the acceptance pass. TypeScript, focused interaction/responsive tests, `git diff --check`, and the production build pass; the repository-wide run passes all 297 then-registered Vitest tests while retaining its known empty-suite files and unrelated categorization assertion. Changes remain uncommitted for extension review.
- Peek rendering follow-up: Manual testing exposed that Preview displayed stored Markdown source and the private `enrichment-meta` header literally. Peek now strips that header and safely renders GitHub-style Markdown (headings, links, lists, tables, quotes, code, and images) with raw HTML disabled. The renderer is lazy-loaded as its own chunk so normal dashboard startup does not absorb its roughly 48 KB compressed cost. Three Peek tests, the neighboring Search/responsive tests, TypeScript, `git diff --check`, and the production build pass; reload the extension and retest the reported X/video example.
- Search Preview consistency follow-up — 2026-08-18: Search had placed Preview inside the selected/full-item action branch, so it was hidden on ordinary rows and also disappeared when workspace actions were available. Every exact Search result now always shows a labelled Preview action; button, double-click, and Enter open the shared Peek by result ID even before the complete item record hydrates. Organization/workspace controls remain selected-item actions. Eight Search tests, three Peek tests, TypeScript, and `git diff --check` pass; extension retest pending.
- Context/Peek checkpoint committed as `51ab22d` (`Add shared item preview and persistent context`). Initial review of the next project-workspace-management problem found three fragmented paths: an icon-only Save-as action inside a project's Workspace view, rename/delete only on the global Workspaces page, and project-workspace creation from browser snapshots in both Workspaces and Tab Commander. There is no clear per-project Create/Manage surface or direct empty-workspace creation. Design agreement is next; no workspace-management code changed after the checkpoint.
- Workspace-management implementation — 2026-08-18: Replaced the ambiguous project Save-as flow with a shared New/Manage workspace dialog used from project and Workspaces pages. New is empty by default with explicit Copy-active, named sets support Activate/Rename/Merge/Delete, merge is same-project/deduplicating and never deletes library items, and Search now switches the active workspace in place without navigation. Added V3-019 and canonical design notes. Focused state/UI tests, TypeScript, `git diff --check`, and the production build pass; live browser QA was unavailable, so manual extension acceptance is next. Changes remain uncommitted.
- V3-019 UI/context follow-up — 2026-08-18: Fixed the concrete toolbar overlap caused by legacy 27px icon widths being applied to new labelled buttons, added responsive manager/Search layouts, and limited passive workspace switchers (project, Search, sidebar) to Global plus open-project workspaces while retaining the already-active workspace for truthful state. Explicit filing pickers remain complete. Focused destination/Home/project/Workspaces/sidebar tests and TypeScript pass; manual visual retest remains.
- V3-019 default-context follow-up — 2026-08-18: Passive workspace switchers now always include Global and the automatic/default project's General workspace, plus General/named workspaces for open project tabs and the truthful active-workspace exception. Full filing pickers remain unchanged.
- V3-019 closed-project correction — 2026-08-18: The active-workspace exception had incorrectly admitted every workspace from the active workspace's closed project. Narrowed it to the exact active workspace key only; other General/named workspaces from that closed project are now hidden.
- V3-019 contextual-default correction — 2026-08-18: Replaced the mistaken always-visible Incoming/default-project workspace with per-context defaults. Project X now defaults to X General, All Library defaults to Global, and an explicit workspace choice is remembered independently for each project context and restored when returning.
