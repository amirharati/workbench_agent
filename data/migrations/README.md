# Migrations

This folder is for short, human-readable notes about schema/data migrations.

Each migration should record:
- DB version bump (e.g., v2 → v3)
- What changed (stores, indexes, field renames)
- How existing data is migrated
- Any manual steps (e.g., “export a backup before upgrading”)


